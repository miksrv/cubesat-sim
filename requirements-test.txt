# Dependencies needed to run the test suite (see tests/).
#
# This intentionally does NOT install requirements.txt as-is: RPi.GPIO,
# lgpio, smbus2 and picamera2 are Raspberry-Pi-specific and either fail to
# install or fail at import time on a generic Linux/CI/dev machine.
# tests/conftest.py replaces them with mocks in sys.modules before any
# src.* module is imported, so the real packages are never needed for
# tests. Everything below is safe to install anywhere.

# Runtime deps exercised for real by the test suite
paho-mqtt
psutil>=5.9.0
transitions
python-dotenv
pyyaml
pyserial
pynmea2
requests

# Test tooling
pytest>=8.0
pytest-cov>=5.0
requests-mock>=1.12
